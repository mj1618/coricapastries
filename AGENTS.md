# Agent instructions

All project guidance for AI agents lives in [CLAUDE.md](CLAUDE.md). Read it first. It covers the
client brief and hard constraints (logo unchanged, factual copy only), the folder layout and
per-concept table, screenshot/QA method with headless Chrome, and Vercel/GitHub deployment
including a CLI gotcha that will silently wipe your login.

Keep CLAUDE.md as the single source of truth. If you learn something future agents need, add it
there rather than here.
